export const tabMenuKeys = Object.freeze([
	"credit-application-management"
] as const);

export const dashboardMenuLabels = Object.freeze({
	"credit-application-management": "Credit Application"
} as const);
